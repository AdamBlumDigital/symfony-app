{% extends template.default %}
{%- block body -%}
{%- embed layout.default -%}
    {%- block content -%}
		<h1>{{ article.title }}</h1>
		<p>{{ article.description }}</p>
		<p>{{ article.id }}</p>
    {%- endblock content -%}
{%- endembed -%}
{%- endblock body -%}
