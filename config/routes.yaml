api_controllers:
    resource: 'routes/api.yaml'
    prefix: /{_locale}/api
    requirements:
      _locale: '%app.locales_supported%'
    defaults:
      _locale: '%app.locales_default%'
view_controllers:
    resource: 'routes/view.yaml'
    prefix: /{_locale}
    requirements:
      _locale: '%app.locales_supported%'
    defaults:
      _locale: '%app.locales_default%'
kernel:
    resource: ../src/Kernel.php
    type: annotation
